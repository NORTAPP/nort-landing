/** @type {import('next').NextConfig} */
const nextConfig = {
  // This tells the landing page to look for its files on the Vercel URL
  // even when it's being viewed on nortapp.online
  assetPrefix: 'https://nort-landing-nine.vercel.app',
};

export default nextConfig;